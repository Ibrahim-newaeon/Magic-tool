version: '3.8'

services:
  magic-tool:
    build: .
    container_name: new-aeon-magic
    ports:
      - "8080:8080"
    environment:
      - GEMINI_API_KEY=${GEMINI_API_KEY}
    volumes:
      - .:/app
      - /app/node_modules
    restart: unless-stopped
